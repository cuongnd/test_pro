/**
 * Created by cuongnd on 9/3/2015.
 */
jQuery(document).ready(function($){
    /*
    $('#articleBody').notebook();
    var ajaxSaveContent=null;
    $('#articleBody').on('contentChange', function(e) {

        var content = e.originalEvent.detail.content;

        if(ajaxSaveContent !== null && typeof ajaxSaveContent === 'object'){

            ajaxSaveContent.abort();
        }
        //console.log(listPositionSetting);
        ajaxSaveContent=$.ajax({
            type: "GET",
            url: this_host+'/index.php',
            data: (function () {
                dataPost = {
                    option: 'com_content',
                    task: 'article.aJaxSaveContent',
                    content:content

                };
                return dataPost;
            })(),
            beforeSend: function () {


                // $('.loading').popup();
            },
            success: function (response) {



            }
        });
        console.log(ajaxSaveContent);
    });
    */
});